FLASK_PORT=5000
FLASK_HOST=0.0.0.0
FLASK_ENV=production
CORS_ORIGINS=http://localhost:3000,http://localhost:4000

# MongoDB Configuration
DB_URL=mongodb+srv://user:password@cluster.mongodb.net/database?retryWrites=true&w=majority
DB_MIN_POOL_SIZE=10
DB_MAX_POOL_SIZE=50

# VNStock configuration
# VNSTOCK_SOURCE: Data source (TCBS, VCI, MSN). Default is TCBS (free, no API key needed)
# VNSTOCK_API_KEY: API key for premium sources (optional)
VNSTOCK_SOURCE=TCBS
# VNSTOCK_API_KEY=your_api_key_here
